{
  "values": {
    "@timestamp": "{{DateRange 30}}",
    "threat.indicator.first_seen": "{{DateRange}}",
    "threat.feed.name": "fakebeat_file",
    "threat.indicator.type": "file",
    "threat.indicator.file.hash.md5": "{{Word}}x{{Word}}",
    "threat.indicator.marking.tlp": "RED",
    "event.type": "indicator",
    "event.category": "threat",
    "event.dataset": "ti_*",
    "event.kind": "enrichment"
  },
  "index": {
    "mappings": {
      "properties": {
        "threat.indicator.type": { "type": "keyword" },
        "threat.indicator.marking.tlp": { "type": "keyword" },
        "threat.feed.name": { "type": "keyword" },
        "threat.indicator.file.hash.md5": { "type": "keyword" },
        "threat.indicator.first_seen": { "type": "DateRange" },
        "event.dataset": { "type": "keyword" },
        "@timestamp": { "type": "DateRange" }
      }
    }
  }
}
