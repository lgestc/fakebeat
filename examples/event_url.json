{
  "values": {
    "@timestamp": "{{DateRange 30}}",
    "url.full": "http://{{Word}}.{{Word}}.{{DomainSuffix}}"
  },
  "index": {
    "mappings": {
      "properties": {
        "url.full": { "type": "wildcard" },
        "@timestamp": { "type": "DateRange" }
      }
    }
  }
}
