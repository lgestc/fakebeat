{
  "values": {
    "@timestamp": "{{date_iso}}",
    "file.hash.md5": "{{word}}x{{word}}"
  },
  "index": {
    "mappings": {
      "properties": {
        "file.hash.md5": { "type": "keyword" },
        "@timestamp": { "type": "date" }
      }
    }
  }
}
