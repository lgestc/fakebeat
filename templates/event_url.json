{
  "values": {
    "@timestamp": "{{date_iso}}",
    "url.full": "http://{{word}}.{{word}}.{{domain}}"
  },
  "index": {
    "mappings": {
      "properties": {
        "url.full": { "type": "wildcard" },
        "@timestamp": { "type": "date" }
      }
    }
  }
}
