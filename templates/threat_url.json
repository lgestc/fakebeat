{
  "values": {
    "@timestamp": "{{date_iso}}",
    "threat.indicator.first_seen": "{{date_iso}}",
    "threat.feed.name": "fakebeat_url",
    "threat.indicator.type": "url",
    "threat.indicator.url.full": "http://{{word}}.{{word}}.{{domain}}",
    "threat.indicator.marking.tlp": "RED",
    "event.type": "indicator",
    "event.category": "threat",
    "event.dataset": "ti_*",
    "event.kind": "enrichment"
  },
  "index": {
    "mappings": {
      "properties": {
        "threat.indicator.type": { "type": "keyword" },
        "threat.indicator.marking.tlp": { "type": "keyword" },
        "threat.feed.name": { "type": "keyword" },
        "threat.indicator.url.full": { "type": "keyword" },
        "threat.indicator.first_seen": { "type": "date" },
        "event.dataset": { "type": "keyword" },
        "@timestamp": { "type": "date" }
      }
    }
  }
}
